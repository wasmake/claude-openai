# Optional comma-separated proxy bearer keys.
# Leave blank to disable auth.
API_KEYS=

# Any non-empty value enables request logging (even "false" or "0").
DEBUG=

# This project does not auto-load .env files; export or source them yourself.
