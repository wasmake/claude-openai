#!/bin/sh
# Publishing is intentionally disabled for claude-openai.
# Use locally:
#   npm ci && npm run build && npm start

set -eu
printf '%s\n' "Publishing is disabled. This project is for local use only."
